{
  "name": "mymeddevices-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:backend": "cd apps/backend && PYTHONPATH=. uv run uvicorn app.main:app --reload",
    "dev:frontend": "turbo dev --filter=vendor",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.1.0"
}
