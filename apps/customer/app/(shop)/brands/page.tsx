import BrandsPage from '../_components/BrandsPage';

export const metadata = {
  title: 'Shop By Brand | MyMedDevices',
  description: 'Browse medical devices by brand — top manufacturers and trusted names in healthcare equipment.',
};

export default function Page() {
  return <BrandsPage />;
}
