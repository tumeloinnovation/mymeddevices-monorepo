import ProductsPage from "./_components/ProductsPage";

export const metadata = {
  title: 'All Products | MyMedDevices',
  description: 'Browse all medical devices and equipment. Shop blood pressure monitors, wheelchairs, nebulizers, diagnostic equipment and more.',
};

export default function Page() {
  return <ProductsPage />;
}
