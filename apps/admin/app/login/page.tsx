import { LoginForm, LoginBackground } from "@mymeddevices/shared-admin"

export default function LoginPage() {
  return (
    <LoginBackground theme="admin">
      <LoginForm theme="admin" />
    </LoginBackground>
  )
}
