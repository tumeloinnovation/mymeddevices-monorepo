import React from "react";
import SearchContent from "@/features/search/components/SearchContent";

const SearchPage = () => {
  return <SearchContent autoFocus={false} />;
};

export default SearchPage;
